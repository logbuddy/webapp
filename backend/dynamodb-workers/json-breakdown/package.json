{
  "name": "herodot-webapp-backend-dynamodb-workers-json-breakdown",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf build && rollup ./src/index.js --file ./build/index.js --format es && pushd build && zip dynamodb_workers_json_breakdown.zip ./index.js && popd"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.18.40",
    "date-fns": "^2.29.3",
    "rollup": "^2.79.1"
  }
}
